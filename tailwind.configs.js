/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        display: "Roboto, ui-serif", // Adds a new `font-display` class
      },
    },
  },
};
